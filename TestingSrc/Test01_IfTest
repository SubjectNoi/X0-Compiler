main {
	integer a, b;
	read(a);
	read(b);
	if (a != 1) {
		write("a != 1");
	}
	if (a == b) {
		write("a == b");
	}
	else {
		write("a != b");
	}
	/* if-if-else still problem, fix later. */
	write(a + b);
}