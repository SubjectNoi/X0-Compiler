main {
	integer a;
	for (a := 1; a != 10; a := a + 1) {
		write(a);
		if (a == 5) {
			/* break; */
		}
	}
    /* multiple for with break still bug */
    write("Now test --");
    for (a := 10; a != 0; a--) {
        write(a);
    }
}