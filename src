main {
	real abc;  				/* this should be ignored */
	int ysy;
	bool flag[123];
	char cc;
	abc := (1 * 2 + 2) < "FUCKYOU";
	ysy := abc + abc * (2 + abc);
	for (i := 1; i < 10; i++) {
		int j;
		j := 10;
		int k;
		k := 20;
		do {
			write(i * j + k);
			if (x >= 10) {
				a := a + 1;
			}
		} while ((j++) > (k--));
	}
	/* at home at home push test */
}
